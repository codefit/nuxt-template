export default defineAppConfig({
  ui: {
    colors: {
      primary: 'brand',
      secondary: 'accent',
      neutral: 'slate',
    },
    button: {
      slots: {
        base: 'cursor-pointer',
      },
    },
  },
})
